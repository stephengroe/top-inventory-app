<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>

    <% if (category_products.length === 0) { %>
      <p>Are you sure you want to delete this category?</p>
      
      <p><strong><%= category.name %></strong></p>
      <p><%= category.description %></p>
      <hr />
      
      <form method="POST" action="">
        <input type="hidden" name="categoryid" value="<%= category._id %>" required='true'>
        <button type="submit">Delete</button>
      </form>
    <% } else { %>
      <p>Before deleting the <%= category.name %> category, you'll need to update the following associated products:</p>

      <% for (const catProduct of category_products) { %>
        <p><strong><%= catProduct.name %></strong></p>
        <p>&pound;<%= catProduct.price %> | <%= catProduct.weight %>lbs. | <%= catProduct.category.name %></p>
        <p>Total in stock: <%= catProduct.number_in_stock %></p>
        <p><%= catProduct.description %></p>
        <p>(<a href='<%= catProduct.url %>/update'>Update</a>)</p>
        <hr />
      <% } %>
    <% } %>
  </body>
</html>
