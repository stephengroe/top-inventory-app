<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include('./partials/header')%>

    <main>
      <h2>Delete Brand</h2>
      <% if (brand_products.length === 0) { %>
        <p>Are you sure you want to delete this brand?</p>
        
        <p><strong><%= brand.name %></strong></p>
        <p><%= brand.description %></p>
        <p><%= brand.address %></p>  
        <hr />
        
        <form method="POST" action="">
          <input type="hidden" name="brandid" value="<%= brand._id %>" required='true'>
          <button type="submit">Delete</button>
        </form>
      <% } else { %>
        <p>Before deleting the brand <%= brand.name %>, you'll need to delete the following associated products:</p>
  
        <% for (const brandProduct of brand_products) { %>
          <p><strong><%= brandProduct.name %></strong></p>
          <p>&pound;<%= brandProduct.price %> | <%= brandProduct.weight %>lbs. | <%= brandProduct.category.name %></p>
          <p>Total in stock: <%= brandProduct.number_in_stock %></p>
          <p><%= brandProduct.description %></p>
          <p>(<a href='<%= brandProduct.url %>/delete'>Delete</a>)</p>
          <hr />
        <% } %>
      <% } %>  
    </main>

    <%- include('./partials/footer') %>
  </body>
</html>